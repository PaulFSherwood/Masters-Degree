# Week 1 — Algorithms and Big-O

WGU’s Applied Algorithms course emphasizes **Big O, benchmarking, profiling, data structures, algorithm design, implementation, and optimization**.

## Day 1 — Big-O, arrays, hash maps

Study:

- O(1), O(log n), O(n), O(n log n), O(n²), O(2ⁿ)
- Arrays vs linked lists
- Hash table lookup, insert, collision idea
- Space complexity

From memory:

```
Write a Big-O table:array accessarray insert middlelinked list insert headhash lookup averagebinary searchmerge sortDFSBFSDijkstra
```

Code:

- Two Sum
- Contains Duplicate
- Valid Anagram
- Merge Sorted Array
